{
  "name": "andwati-com",
  "private": true,
  "devDependencies": {
    "@resvg/resvg-js": "^2.6.2",
    "satori": "^0.29.0",
    "wawoff2": "^2.0.1"
  },
  "scripts": {
    "generate:llms": "node scripts/generate-llms.mjs",
    "generate:og": "node scripts/generate-og-images.mjs",
    "build": "npm run generate:llms && npm run generate:og && zola build"
  }
}
