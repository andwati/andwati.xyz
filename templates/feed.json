{
  "version": "https://jsonfeed.org/version/1.1",
  "title": "{{ config.title | escape_html }}",
  "home_page_url": "{{ config.base_url | escape_html }}",
  "feed_url": "{{ feed_url | escape_html }}",
  "description": "{{ config.description | escape_html }}",
  "authors": [
    {
      "name": "{{ config.extra.author | escape_html }}"
    }
  ],
  "items": [
    {%- for page in pages %}
    {
      "id": "{{ page.permalink | escape_html }}",
      "url": "{{ page.permalink | escape_html }}",
      "title": "{{ page.title | escape_html }}",
      "content_html": {{ page.content | json_encode | safe }},
      "date_published": "{{ page.date | date(format="%Y-%m-%dT%H:%M:%S%:z") }}"
    }{% if not loop.last %},{% endif %}
    {%- endfor %}
  ]
}
